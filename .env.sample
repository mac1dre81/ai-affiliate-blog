# Environment Variables Sample
# Copy to .env.local for local development. Fill in real values.
# For production/CI, use platform secrets (GitHub Actions, Vercel, GCP Secret Manager).
# The app fails fast at startup if a feature flag is enabled but a required key is missing.

# === Core Application ===
NODE_ENV=development
APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
SITE_URL=http://localhost:3000
SESSION_SECRET=replace-with-random-secret-for-production

# === Feature Flags ===
# Enable/disable specific integrations. When true, corresponding API keys below become required.
ENABLE_OPENAI=true
ENABLE_GEMINI=false
ENABLE_ANTHROPIC=false
ENABLE_STRIPE=false
ENABLE_GCP_DEPLOY=false

# === AI Provider Keys (Server-Only) ===
# Keep these strictly server-side. Do NOT expose in client bundles.
# Required if corresponding ENABLE_* flag is true.
OPENAI_API_KEY=sk_test_...replace-with-openai-key...
GOOGLE_API_KEY=...replace-with-google-ai-studio-key...
ANTHROPIC_API_KEY=...replace-with-anthropic-key...

# === Stripe Billing (Server-Only) ===
# Required if ENABLE_STRIPE=true. Get keys from Stripe Dashboard.
STRIPE_SECRET_KEY=sk_test_...replace-with-stripe-secret-key...
STRIPE_WEBHOOK_SECRET=whsec_...replace-with-stripe-webhook-secret...

# === Google Cloud (for deployment engine) ===
# Required if ENABLE_GCP_DEPLOY=true.
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1
GCS_BUCKET=your-gcs-bucket-name

# === Database (optional, if using MongoDB) ===
DATABASE_URL=mongodb://localhost:27017/ai-blog
DATABASE_NAME=ai-blog
